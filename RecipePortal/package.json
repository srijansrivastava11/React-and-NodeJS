{
  "name": "exam02",
  "version": "1.0.0",
  "description": "* start from the up-to-date master branch (`git checkout master; git pull origin master`)\r * Create a feature branch named 'exam2' (`git checkout -b exam2`)\r * modify the `questions.md` file to have the answers required\r * Add any files necessary to create the application below\r * add, commit, and push the branch to github\r * Create a PR to merge to master\r * Be sure to include the reviewer(s)\r * Due by 11:59pm (PT) Sun Jul 5",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.10.3",
    "@babel/preset-env": "^7.10.3",
    "babel-loader": "^8.1.0",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  }
}
